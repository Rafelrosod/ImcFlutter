import 'package:flutter/material.dart';
import 'page/paginaprincipal.dart';

class IMC extends StatelessWidget {
  const IMC({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: HomePage(),
    );
  }
}
